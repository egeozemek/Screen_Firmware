# Display
CONFIG_DISPLAY=y
CONFIG_SSD1306=y

# Switching to SPI instead of I2C
CONFIG_SPI=y

# Memory for display driver framebuffer
CONFIG_HEAP_MEM_POOL_SIZE=2048

# GPIO needed for DC and RES pins
CONFIG_GPIO=y

CONFIG_MAIN_STACK_SIZE=4096

CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE=2048

